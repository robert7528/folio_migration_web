# FOLIO Migration Web Application Configuration

# Application Settings
APP_NAME=FOLIO Migration Web
APP_ENV=development
DEBUG=true

# Server Settings
HOST=127.0.0.1
PORT=8000
# URL prefix when behind nginx proxy (e.g., /folio for https://domain/folio/)
ROOT_PATH=/folio

# Database
DATABASE_URL=sqlite:////folio/folio_migration_web/data/migration.db

# Clients Directory (where client projects are stored)
CLIENTS_DIR=/folio/folio_migration_web/clients

# Migration Template Repository
TEMPLATE_REPO_URL=https://github.com/FOLIO-FSE/migration_repo_template.git

# Encryption Key (auto-generated if not set)
# ENCRYPTION_KEY=

# File Upload Settings
MAX_UPLOAD_SIZE_MB=500
ALLOWED_EXTENSIONS=.mrc,.marc,.iso,.json,.csv,.tsv,.txt,.xml
