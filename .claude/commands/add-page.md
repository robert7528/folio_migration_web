Help me add a new frontend page for: $ARGUMENTS

Follow the existing patterns in the project:
1. Create a Jinja2 template in `templates/`
2. Add necessary JavaScript in `static/js/`
3. Add CSS styles in `static/css/` if needed
4. Add the corresponding API route if needed
5. Add navigation link to the sidebar/menu

Reference existing templates and JS files for UI patterns and style.
